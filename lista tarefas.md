# Lista de Tarefas para Execução por Agentes

Este documento contém os prompts ajustados e ordenados por **ondas de execução**, de acordo com o grafo de dependências do ficheiro `@tasks.md`. 
A análise da outra LLM está **100% correcta**. Não houve qualquer erro na criação do ficheiro `tasks.md`. A numeração das tarefas não dita a cronologia rígida nem obriga a que pertençam à mesma fase. Os identificadores numéricos servem unicamente como "chaves primárias" estáticas. A arrumação (e.g. TASK-15 e TASK-16 na Fase 4) é meramente temática (por se tratarem de integrações externas).

Para construir o sistema com sucesso e sem conflitos, a execução deve seguir as **regras de dependência** (o que é bloqueado pelo quê).
Copie cada bloco de prompt abaixo e forneça-o à IA/agente. Todas as tarefas dentro da mesma onda podem ser executadas por qualquer ordem ou até em paralelo.

---

## Onda 1
### TASK-01 (Fase 1)
```text
Execute a task 01 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 2
### TASK-02 (Fase 1)
```text
Execute a task 02 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 3
### TASK-03 (Fase 1)
```text
Execute a task 03 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-05 (Fase 1)
```text
Execute a task 05 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-10 (Fase 2)
```text
Execute a task 10 da lista da Fase 2, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 4
### TASK-04 (Fase 1)
```text
Execute a task 04 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-06 (Fase 1)
```text
Execute a task 06 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-07 (Fase 1)
```text
Execute a task 07 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-08 (Fase 1)
```text
Execute a task 08 da lista da Fase 1, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-09 (Fase 2)
```text
Execute a task 09 da lista da Fase 2, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-17 (Fase 3)
```text
Execute a task 17 da lista da Fase 3, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### [x] TASK-18 (Fase 3)
```text
Execute a task 18 da lista da Fase 3, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-19 (Fase 3)
```text
Execute a task 19 da lista da Fase 3, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 5
### TASK-11 (Fase 2)
```text
Execute a task 11 da lista da Fase 2, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-15 (Fase 4)
```text
Execute a task 15 da lista da Fase 4, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-16 (Fase 4)
```text
Execute a task 16 da lista da Fase 4, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-21 (Fase 3)
```text
Execute a task 21 da lista da Fase 3, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-35 (Fase 7)
```text
Execute a task 35 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-40 (Fase 6)
```text
Execute a task 40 da lista da Fase 6, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-44 (Fase 7)
```text
Execute a task 44 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-46 (Fase 7)
```text
Execute a task 46 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 6
### TASK-13 (Fase 2)
```text
Execute a task 13 da lista da Fase 2, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-14 (Fase 3)
```text
Execute a task 14 da lista da Fase 3, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-20 (Fase 3)
```text
Execute a task 20 da lista da Fase 3, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-25 (Fase 5)
```text
Execute a task 25 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-41 (Fase 6)
```text
Execute a task 41 da lista da Fase 6, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-42 (Fase 6)
```text
Execute a task 42 da lista da Fase 6, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-45 (Fase 7)
```text
Execute a task 45 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 7
### TASK-12 (Fase 2)
```text
Execute a task 12 da lista da Fase 2, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-22 (Fase 3)
```text
Execute a task 22 da lista da Fase 3, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-23 (Fase 4)
```text
Execute a task 23 da lista da Fase 4, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-26 (Fase 5)
```text
Execute a task 26 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-28 (Fase 5)
```text
Execute a task 28 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-32 (Fase 5)
```text
Execute a task 32 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-33 (Fase 5)
```text
Execute a task 33 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-34 (Fase 5)
```text
Execute a task 34 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-36 (Fase 7)
```text
Execute a task 36 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-37 (Fase 7)
```text
Execute a task 37 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-43 (Fase 6)
```text
Execute a task 43 da lista da Fase 6, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 8
### TASK-24 (Fase 4)
```text
Execute a task 24 da lista da Fase 4, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-49 (Fase 7)
```text
Execute a task 49 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 9
### TASK-27 (Fase 5)
```text
Execute a task 27 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-38 (Fase 7)
```text
Execute a task 38 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-39 (Fase 7)
```text
Execute a task 39 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 10
### TASK-29 (Fase 5)
```text
Execute a task 29 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-30 (Fase 5)
```text
Execute a task 30 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-31 (Fase 5)
```text
Execute a task 31 da lista da Fase 5, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

## Onda 11
### TASK-47 (Fase 7)
```text
Execute a task 47 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-48 (Fase 7)
```text
Execute a task 48 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```

### TASK-50 (Fase 7)
```text
Execute a task 50 da lista da Fase 7, que existe no ficheiro @tasks.md. Todas as tarefas estão relacionadas com a documentação do ficheiro @prdv2.md.
Usa os agentes necessários que estão referenciados em @agents/README.md para a execução de cada task.
Após concluir cada sub tarefa, marque um "X" na subtarefa em @tasks.md.
Marque um "X" ao concluir Fases também.
```
